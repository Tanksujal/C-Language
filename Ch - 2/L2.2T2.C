#include<stdio.h>

main(){
	int width,height,area;
    printf("enter the width for area of rectengle = ");
    scanf("%d",&width);
    printf("enter the height for area of rectengle = ");
    scanf("%d",&height);
	area = width*height;
	printf("the area of rectengle is = %d",area);
}
